# Simpilot Wiki 源文件

此目录保存 GitHub Wiki 的可审阅源文件。每次更新 Wiki 时，应同时提交这里的对应 Markdown，避免 Wiki 内容只存在于独立仓库。

页面文件名与 GitHub Wiki 页面标题一致。中文为默认内容；英文页面使用 `.en-US.md` 后缀，繁体中文页面使用 `.zh-TW.md` 后缀。每个用户页面顶部均须提供三种语言的切换链接。
